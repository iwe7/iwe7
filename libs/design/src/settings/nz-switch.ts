import { DesignAndPreviewProps } from './types';

const data: DesignAndPreviewProps[] = [
  {
    name: 'nzSize',
    label: '尺寸',
    value: 'default',
    type: 'select',
    options: ['default', 'small']
  }
];

export default data;
